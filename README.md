# dsh-taskflow · AI 合同式任务看板

> DeepSeek Harness (dsh) Web 插件 —— 你创建任务（验收标准可给可不给），AI 自己拆解、自己实现、自己举证；人来验收，通过才算 done，不通过打回继续迭代，全过程状态留痕。

## 定位一句话

**人与 AI 之间的合同管理器**：卡片不是提示词的容器，而是一份合同（做什么 + 怎么算做完 + 准许动用什么）；看板是这块共享黑板的实时视图；调度器的职责是把「就绪的合同」喂给会话，验收人的职责是让「done」两个字有含金量。

## 与现有能力的分工

| 能力 | 层面 | taskflow 的关系 |
|---|---|---|
| `/goal` | 会话内完成驱动循环 | 不抢它的活：卡片运行时，会话内部照常用 goal |
| `/schedule` | 会话内定时提醒 | 不重叠 |
| `dsh-task-board`（第三方） | cron 作业发射器 | 独立新项目，数据目录分离，可共存；本项目吸收其教训重新设计 |

## 文档导航

| 文档 | 内容 |
|---|---|
| [`docs/REQUIREMENTS.md`](docs/REQUIREMENTS.md) | 需求文档：背景、定位、用户旅程、用户故事、需求清单（FR/NFR）、里程碑、风险 |
| [`docs/FUNCTIONS.md`](docs/FUNCTIONS.md) | 功能文档：概念模型、数据模型、状态机、逐项功能规格、UI/UX 规格、API 草案、安全模型 |

## 当前状态

🔗 **任务接续 / 血缘 DAG 已实现**（2026-09-22，方案与预览见
[`docs/PLAN-FOLLOWUP.md`](docs/PLAN-FOLLOWUP.md)）——done 卡「接续新任务」 /
review「确认并接续」签新合同而非重开老合同（状态机零改动）：`basedOn` 建卡
（父须 done、禁环、上限 10）、pins 缺省继承第一父、逐父独立交接摘要注入拆解
提示、`parentIds`/`depth` 落库；客户端：可搜索多选继承下拉、合同 tab 接续横幅、
流程 tab 上一棒节点、看板链徽标与血缘连线层（线在卡上层 + 实心箭头贴目标卡）、
族谱视图。

🚧 **M1 已实现**（宿主核心 + 客户端 + dsh 集成层，测试与三门禁全绿）——
见 [`docs/ACCEPTANCE-M1.md`](docs/ACCEPTANCE-M1.md)（自验收报告：逐 FR/NFR 证据与待真机复验清单）。

🎨 **UI 方案三「分栏」已落地**（真机主链路通过，用户报的 3 个问题已修）。

🪟 **验收工作台弹窗改版已落地**（2026-09-10 深夜）：任务详情从 480px 侧栏抽屉升级为
居中大弹窗（窄屏退化全屏、Esc/焦点陷阱、任务操作吸底）。

⚖️ **验收语义升级已落地**（2026-09-11，两期连做）：**验收只对合同，过程归 AI**——
①子任务全部完成后 AI 自动跑「任务级终检」，对照任务级验收标准产出整体交付证据卡
（失败重试 1 次，仍失败兜底进 review 可人工补跑）；②验收工作台重排：任务级判定面优先，
子任务降级为「执行过程举证」折叠附录（标注无需逐个验收）；③打回不再让人选子任务：
人只给批语，AI triage 定位返工范围（未点名的不重跑，定位失败回退全量）。
规格见 `docs/FUNCTIONS.md` §4.5/§4.6/§4.7。

🔒 **执行权限与审批通知已实现**（M2 第一批）：创建时可选「完全权限 / 需要审批」，
提权审批落库 + 全局通知栏 + 一键跳转裁决，执行会话看门狗防无声挂起——
方案与决策记录见 [`docs/PLAN-APPROVAL.md`](docs/PLAN-APPROVAL.md)。

🔀 **全局模型设置已实现**（M2 第二批）：工具栏齿轮浮层，拆解/执行两槽模型独立配置
（默认跟随宿主），目录投影自宿主 `ctx.llm`，即改即存，时间线/证据卡留痕所用模型——
方案与决策记录见 [`docs/PLAN-MODEL.md`](docs/PLAN-MODEL.md)。

🩹 **2026-09-10 真机事故修复完成并上线**：调度器孤儿会话死锁（终态任务残留 in-progress
子任务占满 WIP 额度）、完全权限仍被要求授权、迭代上限默认 3 致中途拦人、看板冷启动慢——
四项全修（含回归测试），真机 12 子任务已全量跑完待人工终批。详情见
[`docs/HANDOFF-2026-09-10.md`](docs/HANDOFF-2026-09-10.md)（交接记录：待办、未提交清单与提交拆分建议）。

📦 **交付物一等公民已实现**（2026-09-11，§4.5b）：证据可结构化声明 artifacts（产物本体：
绝对路径 + 说明 + 核验方式）；任务级终检必须声明文件交付物；验收台判定面顶部新增
「交付物」区，宿主只读预览路由（≤256KiB 截断、二进制拒显、仅放行声明过的路径）
+ 受限 Markdown 渲染，验收时直接在弹窗里读 AI 落盘的报告，不用离开看板翻文件。

🩹 **2026-09-11 下午真机事故修复与口径收敛已提交**（`9f2bf36`，已推送）：
终检重试两态统一、拒收 problems 完整留痕、重跑终检入口、artifact 预览路由漏注册回归、
交付物层级口径（验收栏只放最终产物，上限 10→20）、子任务进度 done+review、demo 退役清理。

🎨 **验收工作台改版已落地**（2026-09-11，方案B）：判定面 = 结论带（通过率/差额/进度条，整条语义底色）+ 去重合并的逐条核验（验收标准只渲染一份，pass 绿条 / partial 橙底 / fail 红底加粗）+ 判定面白卡蓝左条与过程举证无卡发丝线的主次分家；设计预览稿见 `design/review-preview.html`（三方向：基线/方案B/方案B′）。

🩹 **2026-09-12 真机回归三件已落地**：①卡片「执行需确认」只看当前状态（in-progress 且未确认）——此前事件史留过一次等待痕的 done 任务会永远显示该 chip；②**产物独立成页**——新增「产物」tab（任务级交付物独立呈现 + 只读预览），验收页专注验收：review 默认落验收页，验收完成（done）后默认落**产物页**，不用再进验收页翻产物；③（测试加固）冒烟中按文本定位 tab、等 UI 同步后再断言，去掉 nth-child 与竞态脆弱点。

🛠️ **2026-09-12 交互小改进（已提交 `c53e2be`）**：①侧栏「任务看板」入口**复刻宿主「新会话」nav 根类**——尺寸/间距/圆角与宿主完全一致（含 rail 折叠态），不再偏小；②**卡片归档（需确认）+ 批量归档**——已完成/已取消卡片右上角「归档 → 确认归档/取消」两段式；工具栏「批量归档」进入勾选模式（仅可归档状态可勾），批量条「归档所选 → 确认归档 n 项」二次确认后逐个归档，archived 移出主视图。

🏁 **M2 收口（2026-09-15，FR-12/13/14/16 全部落地）**：
①**依赖 DAG 调度守卫**（FR-12）默认启用——就绪判据对齐验收语义（直接依赖
产物已存在 = done 或 review=举证完毕，否则 DAG 必然死锁）；打回/triage 定位的
返工集沿 deps 传递闭包**回退下游**（运行中终止会话、举证完毕作废重排；done
不动只留复核提示；未牵连分支不动）。
②**WIP 并发上限**（FR-13）进全局设置（1–8，settings.json 持久化，设置浮层
「调度」槽）——提高即改即生效放行排队，降低不杀运行中会话；子任务列表
「等依赖：X / 排队中」chip 队列可视化。
③**批量验收**（FR-14）：「批量操作」模式扩到待验收卡——通过所选（两段式）、
打回所选（共用批语必填，走 AI triage）、归档所选，动作只作用于各自可用子集。
④**浏览器通知**（FR-16）：shell 级常驻 watcher，新进待验收/新发起审批发系统
通知（首帧基线不轰炸），点击定位任务/审批卡；工具栏 🔔 opt-in。
⑤**顺带修复真 bug**：PUT settings 此前只改引擎内存从不落盘（重启即失模型
选择），现先落盘后生效（fail-closed）。
⑥**测试基建**：清掉上会话误提交的调试残渣；权限门用例的瞬态检测信号改持久
sessionIds 落号（2026-09-15 偶发超时根因）。FR-18 经裁决由任务级终检覆盖，
口径见 REQUIREMENTS §5。

🏁 **M3 落地（2026-09-15，FR-17/19/20）**：
①**触发器**（FR-17）：plugin config `schedules[]` cron 定时建卡（5 字段标准
cron，分钟去重 + 幂等 requestId，重启不重发；无人值守：定时建卡 → 自动推进 →
等验收）+ `webhookToken` 门控的 `POST /api/taskflow/hook` 远程建卡（CI/脚本）；
file-watch 裁决不做（watcher 面/去抖复杂，价值最弱，文档留痕）。
②**模板库**（FR-19）：templates.json 播种 4 内置模板（修 Bug/新功能/技术调研/
清理整理，带验收标准草案）；创建抽屉 chip 一键预填、内联「存为模板」、两段式
删除——创建 30 秒内完成的助推。
③**周期统计**（FR-20）：工具栏统计浮层——7/30 天吞吐、累计完成、一次通过率、
平均迭代轮次、拆解采纳率（直接对齐 §9 成功指标口径），空账本显示「—」不编造，
口径注脚与数字同屏。

至此 PRD 的 FR 清单全量收口（FR-18 裁决覆盖、file-watch 裁决不做）。
新批次待真机复验：DAG 并发调度、批量验收、浏览器通知、统计、cron/webhook、
工作目录钉定、能力预设、任务级并发/模型覆盖（模板 UI 已随 FR-22 退役，后端保留）。

🆕 **FR-21 工作区域钉定 + 新建弹窗 Bento 改版（2026-09-20）**：
①创建任务可指定「工作目录」（`pins.workspace`，绝对路径，留空 = 宿主默认）——
硬约束（会话 `meta.cwd` 钉定、沙箱可写根随之限定，目录不存在 fail-closed）+
软约束（拆解/执行/终检提示词注入「非必要改动不出区域」条款），合同卡展示钉定区域；
工作目录用**目录选择器**（`GET /api/taskflow/dirs` 只读浏览：上级/下钻/选定，
不留自由文本入口；插件层路由漏注册致宿主 404、目录选择器全挂的回归已修并加测试）；
②新建任务从侧栏抽屉升级为**居中大弹窗 + Bento 单列**（任务/执行两格白卡、
焦点陷阱、Esc/遮罩关闭、窄屏全屏），提交门禁禁用时显示具体原因。

🆕 **FR-22/23/24 创建表单能力化 + 任务级覆盖（2026-09-20）**：
①**能力预设（FR-22）**：创建表单 4 个能力 chip（修 Bug / 新功能 / 技术调研 /
清理整理），单选可取消——**只进后台不进正文**：`Task.capability` 落库（白名单校验），
拆解提示词注入对应口径（怎么拆、验收标准往哪个方向起草），正文/验收编辑框不出现
预设文字；FR-19 模板库后端（templates.json / 路由）保留，创建表单不再使用。
②**任务级并发数（FR-23）**：创建表单数字输入（默认 = 全局设置当前值，1–8 校验）
→ `Task.maxConcurrentSubtasks`；调度器实际取 **min(全局上限, 任务级上限)**——
只收紧该任务内子任务的并行度，不突破全局 WIP。
③**任务级模型覆盖（FR-24）**：创建表单模型下拉（数据源既有
`GET /api/taskflow/models` 目录，按 provider 分组，同全局设置浮层形态），默认
「跟随全局设置」；选定后落 `Task.model`，该任务的拆解 / 执行 / 终检会话统一用它，
不随全局两槽。
④迭代上限改预设档位单选（30/60/90/无限，默认不限；档位须在协议白名单 [1,99] 内）。

🆕 **子任务 DAG 流程图（2026-09-21，FR-12 可视化，三期迭代）**：任务详情弹窗
**「流程」tab**——点进执行中的任务（in-progress 默认落点）即可看到正在跑的
执行流程：图 = 完整任务管线 **AI 拆解 → 子任务 DAG → AI 终检 → 人工终批**
（首尾三个药丸相位节点，拆解中不再是空占位——头节点呼吸即「正在拆解」）；
子任务最长路径分层、左→右连线，状态色对齐宿主语言（执行中琥珀呼吸、待核验蓝、
完成绿、受阻红、等依赖灰），指向运行中会话的连线虚线流动，等待行复用
「等依赖：X / 排队中」投影。**三期（同日）**：历史 tab 退役，轨迹内嵌流程图
——点击任意节点（子任务卡 / 阶段药丸）在图下方展开它的执行轨迹，执行中的
节点实时滚动追加 AI 进度便签，已完成的显示静态历史（任务事件按相位分流，
终批面板兜底零盲区）。数据零新增，纯前端实现、零新依赖（不引 dagre，
布局与分流均纯函数可单测）。规格见 `docs/FUNCTIONS.md` §4.8/§4.17。

🆕 **流程 tab 体验改版 + Token 用量回采（2026-09-22）**：
①流程 tab 改填充式布局——与验收台同走 `tf-modal-body-fill`，DAG 图区固定 340px，
轨迹面板弹性占满剩余空白，底部常驻节点基本信息栏（状态/轮次/会话/执行时间/
Token/模型/依赖；执行时间从首次开工起算，不含编排等待）；
②DAG **蛇形多行布局**——槽位序列按真实数据推导（拆解+子任务分层+终检+终批），
每 4 槽折行、方向交替，宽度恒定 ~1000px 零缩放零横滚，字号不缩水，折返处竖直
S 弧连线；③弹窗底部拖拽手柄拉高弹窗，多出高度全归弹性块（轨迹面板/判定面）；
④**Token 用量回采**——执行/接管会话在 finally 自会话事件（assistant/message 的
usage）累加，随 outcome 回引擎落库 `Subtask.tokenUsage`（历次累计），崩溃/打断
也尽量留痕；provider 不回报则显示「—」。

## 开发

```sh
npm install          # .npmrc 已带 legacy-peer-deps（dsh 包 npm peer 版本错位）
npm run typecheck    # strict 类型检查（宿主面 + 客户端面）
npm run test         # 单测 + E2E 主流程 + 客户端渲染冒烟
npm run build        # dist/ 全产物（含 dsh 客户端模块 bundle）
```

安装到 dsh 宿主（需真实宿主环境）：`dsh plugin --profile <name> add dsh-taskflow`。

### 代码地图

| 层 | 位置 | 说明 |
|---|---|---|
| protocol | `src/protocol/` | ledger schema v1、action 白名单、拆解输出/证据校验（纯 TS，零依赖） |
| host | `src/host/` | ledger 原子存储、状态机（T1–T10/S1–S6）、引擎（调度/证据门/打回迭代/恢复）、HTTP+SSE、dsh 适配器与插件入口 |
| client | `src/client/` | 看板/抽屉/验收页/时间线（React + `--dsw-alias-*` 主题变量）、纯视图模型、HTTP/SSE 传输 |

## 仓库

- GitHub：`LuckVd/dsh-taskflow`
- 未来发布形态：dsh Web 插件（npm 包），目标宿主 `dsh 0.1.2-rc.1+`
